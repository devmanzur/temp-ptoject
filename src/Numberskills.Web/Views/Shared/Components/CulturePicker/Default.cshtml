@model Numberskills.Web.ViewComponents.CulturePickerModel

<div>
    <form method="GET" style="margin-bottom: 0">
        <div class="">
            <label class="sr-only" for="culture-picker">
                Culture
            </label>
            <select
                id="culture-picker"
                class="form-control"
                name="culture"
                onchange="this.parentElement.parentElement.submit();">
                @foreach (var culture in Model.SupportedCultures)
                {
                    <option
                        value="@culture.Name"
                        selected="@(Model.CurrentUICulture.Name == culture.Name)">
                        @culture.DisplayName - @culture.NativeName
                    </option>
                }
            </select>
        </div>
    </form>
</div>